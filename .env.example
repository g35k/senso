# Supabase (project Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: where users land after email confirmation (defaults to /login on this origin)
# VITE_SUPABASE_EMAIL_REDIRECT_TO=http://localhost:5173/login

# Optional: where password reset links send users (defaults to /login on this origin)
# VITE_SUPABASE_PASSWORD_RESET_REDIRECT_TO=http://localhost:5173/login
